---
title: "Интерфейс WebChat: AI-помощник в браузере | Руководство Clawdbot"
sidebarTitle: "Попробуйте веб-версию AI"
subtitle: "Интерфейс WebChat: AI-помощник в браузере"
description: "Научитесь использовать встроенный интерфейс WebChat в Clawdbot для общения с AI-помощником. Это руководство охватывает способы доступа к WebChat, основные функции (управление сессиями, загрузка вложений, поддержка Markdown) и настройку удаленного доступа (SSH-туннель, Tailscale) без дополнительных портов или отдельной конфигурации."
tags:
  - "WebChat"
  - "браузерный интерфейс"
  - "чат"
prerequisite:
  - "start-gateway-startup"
order: 150
---

# Интерфейс WebChat: AI-помощник в браузере

## Чему вы научитесь

После завершения этого руководства вы сможете:

- ✅ Доступ к интерфейсу WebChat через браузер
- ✅ Отправлять сообщения в WebChat и получать ответы AI
- ✅ Управлять историей сессий и переключаться между сессиями
- ✅ Загружать вложения (изображения, аудио, видео)
- ✅ Настраивать удаленный доступ (Tailscale/SSH-туннель)
- ✅ Понимать разницу между WebChat и другими каналами

## Ваши текущие трудности

Возможно, вы уже запустили Gateway, но хотите иметь более интуитивный графический интерфейс для общения с AI-помощником, а не только командную строку.

Возможно, вы хотите знать:

- "Есть ли веб-интерфейс, похожий на ChatGPT?"
- "В чем разница между WebChat и каналами WhatsApp/Telegram?"
- "Нужна ли отдельная конфигурация для WebChat?"
- "Как использовать WebChat на удаленном сервере?"

Хорошая новость: **WebChat — это встроенный в Clawdbot чат-интерфейс**, который не требует отдельной установки или настройки и доступен сразу после запуска Gateway.

## Когда использовать этот метод

Когда вам нужно:

- 🖥️ **Графический интерфейс для общения**: предпочтение чату в браузере вместо командной строки
- 📊 **Управление сессиями**: просмотр истории, переключение между разными сессиями
- 🌐 **Локальный доступ**: общение с AI на том же устройстве
- 🔄 **Удаленный доступ**: доступ к удаленному Gateway через SSH/Tailscale-туннель
- 💬 **Интерактив в формате Rich Text**: поддержка Markdown и вложений

---

## 🎒 Подготовка перед началом

Перед использованием WebChat убедитесь:

### Необходимые условия

| Условие | Как проверить |
| ---------------------- | ------------------------------------------- |
| **Gateway запущен** | `clawdbot gateway status` или проверить, работает ли процесс |
| **Порт доступен** | Убедитесь, что порт 18789 (или пользовательский порт) не занят |
| **AI-модель настроена** | `clawdbot models list` — проверить наличие доступных моделей |

::: warning Предварительные уроки
Это руководство предполагает, что вы уже завершили:
- [Быстрый старт](../../start/getting-started/) — установка, настройка и запуск Clawdbot
- [Запуск Gateway](../../start/gateway-startup/) — понимание различных режимов запуска Gateway

Если вы еще не завершили эти уроки, вернитесь к ним сначала.
:::

### Опционально: настройка аутентификации

WebChat по умолчанию требует аутентификацию (даже при локальном доступе) для защиты вашего AI-помощника.

Быстрая проверка:

```bash
## Просмотр текущей конфигурации аутентификации
clawdbot config get gateway.auth.mode
clawdbot config get gateway.auth.token
```

Если не настроено, рекомендуется сначала установить:

```bash
## Настройка token-аутентификации (рекомендуется)
clawdbot config set gateway.auth.mode token
clawdbot config set gateway.auth.token your-secure-token-here
```

Подробное описание: [Конфигурация аутентификации Gateway](../../advanced/security-sandbox/).

---

## Основная концепция

### Что такое WebChat

**WebChat** — это встроенный в Clawdbot чат-интерфейс, который напрямую взаимодействует с AI-помощником через Gateway WebSocket.

**Ключевые особенности**:

```
┌─────────────────────────────────────────────────────┐
│              Архитектура WebChat                    │
├─────────────────────────────────────────────────────┤
│                                                     │
│  Браузер/клиент                                     │
│      │                                              │
│      ▼                                              │
│  Gateway WebSocket (ws://127.0.0.1:18789)          │
│      │                                              │
│      ├─ chat.send → Agent → обработка сообщения     │
│      ├─ chat.history → возврат истории сессии      │
│      ├─ chat.inject → добавление системной заметки │
│      └─ поток событий → обновления в реальном времени│
│                                                     │
└─────────────────────────────────────────────────────┘
```

**Различия с другими каналами**:

| Особенность | WebChat | WhatsApp/Telegram и др. |
| ------------ | -------------------------------- | ------------------------------ |
| **Способ подключения** | Прямой доступ к Gateway через браузер | Требуется стороннее приложение и вход |
| **Требования к настройке** | Без отдельной настройки, используется порт Gateway | Требуется API Key/Token для конкретного канала |
| **Маршрутизация ответов** | Детерминированная маршрутизация обратно в WebChat | Маршрутизация в соответствующий канал |
| **Удаленный доступ** | Через SSH/Tailscale-туннель | Удаленный доступ предоставляется платформой канала |
| **Модель сессии** | Использует управление сессиями Gateway | Использует управление сессиями Gateway |

### Как работает WebChat

WebChat не требует отдельного HTTP-сервера или настройки портов, он напрямую использует WebSocket-сервис Gateway.

**Ключевые моменты**:
- **Общий порт**: WebChat использует тот же порт, что и Gateway (по умолчанию 18789)
- **Без дополнительной настройки**: нет специального блока настройки `webchat.*`
- **Синхронизация в реальном времени**: история запрашивается из Gateway в реальном времени, локально не кэшируется
- **Только чтение**: если Gateway недоступен, WebChat переходит в режим только для чтения

::: info WebChat vs Control UI
WebChat фокусируется на чат-опыте, а **Control UI** предоставляет полную панель управления Gateway (настройка, управление сессиями, управление навыками и др.).

- WebChat: `http://localhost:18789/chat` или чат-представление в приложении macOS
- Control UI: `http://localhost:18789/` — полная панель управления
:::

---

## Пошаговое руководство

### Шаг 1: Доступ к WebChat

**Зачем это нужно**
WebChat — это встроенный в Gateway чат-интерфейс, не требующий установки дополнительного программного обеспечения.

#### Способ 1: Доступ через браузер

Откройте браузер и перейдите по адресу:

```bash
## Адрес по умолчанию (использует порт по умолчанию 18789)
http://localhost:18789

## Или используйте loopback-адрес (более надежно)
http://127.0.0.1:18789
```

**Что вы должны увидеть**:
```
┌─────────────────────────────────────────────┐
│          Clawdbot WebChat                   │
│                                             │
│  ┌───────────────────────────────────┐   │
│  │  [Список сессий]  [Настройки]      │   │
│  └───────────────────────────────────┘   │
│                                             │
│  ┌───────────────────────────────────┐   │
│  │  Привет! Я ваш AI-помощник.       │   │
│  │  Чем я могу вам помочь?           │   │
│  └───────────────────────────────────┘   │
│                                             │
│  [Введите сообщение...]              [Отправить]   │
└─────────────────────────────────────────────┘
```

#### Способ 2: Приложение macOS

Если вы установили приложение Clawdbot для меню macOS:

1. Нажмите на иконку в меню
2. Выберите "Open WebChat" или нажмите на иконку чата
3. WebChat откроется в отдельном окне

**Преимущества**:
- Нативный опыт macOS
- Поддержка горячих клавиш
- Интеграция с Voice Wake и Talk Mode

#### Способ 3: Быстрый доступ из командной строки

```bash
## Автоматически открыть браузер на WebChat
clawdbot web
```

**Что вы должны увидеть**: браузер по умолчанию автоматически открывается и переходит на `http://localhost:18789`

---

### Шаг 2: Отправьте первое сообщение

**Зачем это нужно**
Проверить, работает ли соединение между WebChat и Gateway, и правильно ли AI-помощник отвечает.

1. Введите ваше первое сообщение в поле ввода
2. Нажмите кнопку "Отправить" или нажмите `Enter`
3. Наблюдайте за ответом в чат-интерфейсе

**Пример сообщения**:
```
Hello! I'm testing WebChat. Can you introduce yourself?
```

**Что вы должны увидеть**:
```
┌─────────────────────────────────────────────┐
│  Вы → AI: Hello! I'm testing...      │
│                                             │
│  AI → Вы: Привет! Я Clawdbot AI-помощник.│
│  Я могу помочь вам ответить на вопросы,     │
│  написать код, управлять задачами и т. д.   │
│  Чем я могу вам помочь?           │
│                                             │
│  [Введите сообщение...]              [Отправить]   │
└─────────────────────────────────────────────┘
```

::: tip Совет по аутентификации
Если Gateway настроен с аутентификацией, при доступе к WebChat будет предложено ввести token или пароль:

```
┌─────────────────────────────────────────────┐
│          Аутентификация Gateway            │
│                                             │
│  Введите Token:                             │
│  [•••••••••••••]              │
│                                             │
│              [Отмена]  [Войти]              │
└─────────────────────────────────────────────┘
```

Введите настроенный вами `gateway.auth.token` или `gateway.auth.password`.
:::

---

### Шаг 3: Используйте функции WebChat

**Зачем это нужно**
WebChat предлагает богатые функции интерактивности, знание которых улучшит опыт использования.

#### Управление сессиями

WebChat поддерживает управление несколькими сессиями, что позволяет вам общаться с AI в разных контекстах.

**Шаги**:

1. Нажмите на список сессий слева (или кнопку "Новая сессия")
2. Выберите или создайте новую сессию
3. Продолжите диалог в новой сессии

**Особенности сессий**:
- ✅ Независимый контекст: история сообщений каждой сессии изолирована
- ✅ Автоматическое сохранение: все сессии управляются Gateway, постоянное хранилище
- ✅ Синхронизация между платформами: одна и та же история сессий используется CLI, WebChat, приложением macOS, узлами iOS/Android

::: info Основная сессия
WebChat по умолчанию использует **ключ основной сессии** (`main`) Gateway, что означает, что все клиенты (CLI, WebChat, приложение macOS, узлы iOS/Android) совместно используют одну и ту же историю основной сессии.

Если вам нужен изолированный контекст, вы можете установить разные ключи сессии в настройке.
:::

#### Загрузка вложений

WebChat поддерживает загрузку изображений, аудио, видео и других вложений.

**Шаги**:

1. Нажмите на иконку "Вложение" рядом с полем ввода (обычно 📎 или 📎️)
2. Выберите файл для загрузки (или перетащите файл в область чата)
3. Введите соответствующее текстовое описание
4. Нажмите "Отправить"

**Поддерживаемые форматы**:
- 📷 **Изображения**: JPEG, PNG, GIF
- 🎵 **Аудио**: MP3, WAV, M4A
- 🎬 **Видео**: MP4, MOV
- 📄 **Документы**: PDF, TXT и др. (зависит от настройки Gateway)

**Что вы должны увидеть**:
```
┌─────────────────────────────────────────────┐
│  Вы → AI: Пожалуйста, проанализируйте это изображение │
│  [📎 photo.jpg]                            │
│                                             │
│  AI → Вы: Я вижу, что это...        │
│  [Результат анализа...]                     │
└─────────────────────────────────────────────┘
```

::: warning Ограничение размера файла
WebChat и Gateway имеют ограничение на размер загружаемых файлов (обычно несколько МБ). Если загрузка не удается, проверьте размер файла или настройку мультимедиа в Gateway.
:::

#### Поддержка Markdown

WebChat поддерживает формат Markdown, что позволяет форматировать сообщения.

**Пример**:

```markdown
# Заголовок
## Подзаголовок
- Пункт списка 1
- Пункт списка 2

**Жирный** и *курсив*
`код`
```

**Предварительный просмотр**:
```
# Заголовок
## Подзаголовок
- Пункт списка 1
- Пункт списка 2

**Жирный** и *курсив*
`код`
```

#### Командные сокращения

WebChat поддерживает команды со слэшем для быстрого выполнения определенных операций.

**Часто используемые команды**:

| Команда | Функция |
| ---------------- | ---------------------------- |
| `/new` | Создать новую сессию |
| `/reset` | Сбросить историю текущей сессии |
| `/clear` | Очистить все сообщения текущей сессии |
| `/status` | Показать статус Gateway и каналов |
| `/models` | Перечислить доступные AI-модели |
| `/help` | Показать справочную информацию |

**Примеры использования**:

```
/new
## Создать новую сессию

/reset
## Сбросить текущую сессию
```

---

### Шаг 4 (опционально): настройка удаленного доступа

**Зачем это нужно**
Если вы запускаете Gateway на удаленном сервере или хотите получить доступ к WebChat с других устройств, необходимо настроить удаленный доступ.

#### Доступ через SSH-туннель

**Подходящий сценарий**: Gateway на удаленном сервере, вы хотите получить доступ к WebChat с локального компьютера.

**Шаги**:

1. Создайте SSH-туннель, который отображает удаленный порт Gateway на локальный:

```bash
## Отображение удаленного порта 18789 на локальный порт 18789
ssh -L 18789:localhost:18789 user@your-remote-server.com
```

2. Держите SSH-соединение открытым (или используйте параметр `-N` для выполнения удаленных команд)

3. В локальном браузере перейдите по адресу: `http://localhost:18789`

**Что вы должны увидеть**: тот же интерфейс WebChat, что и при локальном доступе

::: tip Сохранение SSH-туннеля
SSH-туннель перестает работать при разрыве соединения. Если вам нужен постоянный доступ:

- Используйте `autossh` для автоматического переподключения
- Настройте `LocalForward` в конфигурации SSH
- Используйте systemd/launchd для автоматического запуска туннеля
:::

#### Доступ через Tailscale

**Подходящий сценарий**: использование Tailscale для создания частной сети, Gateway и клиенты в одном tailnet.

**Шаги настройки**:

1. Включите Tailscale Serve или Funnel на машине с Gateway:

```bash
## Редактировать файл конфигурации
clawdbot config set gateway.tailscale.mode serve
## Или
clawdbot config set gateway.tailscale.mode funnel
```

2. Перезапустите Gateway

```bash
## Перезапуск Gateway для применения настройки
clawdbot gateway restart
```

3. Получите Tailscale-адрес Gateway

```bash
## Просмотр статуса (покажет Tailscale URL)
clawdbot gateway status
```

4. Доступ с устройства-клиента (в том же tailnet):

```
http://<gateway-tailscale-name>.tailnet-<tailnet-id>.ts.net:18789
```

::: info Tailscale Serve vs Funnel
- **Serve**: доступен только в tailnet, безопаснее
- **Funnel**: публичный доступ в Интернет, требуется защита `gateway.auth`

Рекомендуется использовать режим Serve, если вам не нужен доступ из общедоступной сети.
:::

#### Аутентификация удаленного доступа

Независимо от того, используете ли вы SSH-туннель или Tailscale, если Gateway настроен с аутентификацией, вам все равно потребуется предоставить token или пароль.

**Проверьте настройку аутентификации**:

```bash
## Просмотр режима аутентификации
clawdbot config get gateway.auth.mode

## Если режим token, подтвердите, что token установлен
clawdbot config get gateway.auth.token
```

---

## Контрольная точка ✅

После выполнения описанных выше шагов вы должны быть able to:

- [ ] Доступ к WebChat в браузере (`http://localhost:18789`)
- [ ] Отправлять сообщения и получать ответы AI
- [ ] Использовать функции управления сессиями (создание, переключение, сброс сессий)
- [ ] Загружать вложения и позволить AI анализировать их
- [ ] (Опционально) Удаленный доступ к WebChat через SSH-туннель
- [ ] (Опционально) Доступ к WebChat через Tailscale

::: tip Проверка соединения
Если WebChat недоступен или отправка сообщений не удается, проверьте:

1. Запущен ли Gateway: `clawdbot gateway status`
2. Правильный ли порт: подтвердите, что вы обращаетесь к `http://127.0.0.1:18789` (а не `localhost:18789`)
3. Настроена ли аутентификация: `clawdbot config get gateway.auth.*`
4. Просмотр подробных журналов: `clawdbot gateway --verbose`
:::

---

## Предупреждения о типичных проблемах

### ❌ Gateway не запущен

**Неправильный способ**:
```
Прямой доступ к http://localhost:18789
## Результат: сбой соединения или невозможность загрузки
```

**Правильный способ**:
```bash
## Сначала запустите Gateway
clawdbot gateway --port 18789

## Затем получите доступ к WebChat
open http://localhost:18789
```

::: warning Сначала запустите Gateway
WebChat зависит от WebSocket-сервиса Gateway. Без запущенного Gateway WebChat не может работать нормально.
:::

### ❌ Неправильная настройка порта

**Неправильный способ**:
```
Доступ к http://localhost:8888
## Gateway фактически работает на порту 18789
## Результат: соединение отклонено
```

**Правильный способ**:
```bash
## 1. Просмотр фактического порта Gateway
clawdbot config get gateway.port

## 2. Доступ с использованием правильного порта
open http://localhost:<gateway.port>
```

### ❌ Пропущена настройка аутентификации

**Неправильный способ**:
```
Не настроены gateway.auth.mode или token
## Результат: WebChat сообщает о сбое аутентификации
```

**Правильный способ**:
```bash
## Настройка token-аутентификации (рекомендуется)
clawdbot config set gateway.auth.mode token
clawdbot config set gateway.auth.token your-secure-token-here

## Перезапуск Gateway
clawdbot gateway restart

## Введите token при доступе к WebChat
```

### ❌ Не настроен удаленный доступ

**Неправильный способ**:
```
Прямой доступ к IP-адресу удаленного сервера с локального компьютера
http://remote-server-ip:18789
## Результат: время ожидания соединения (блокировка брандмауэром)
```

**Правильный способ**:
```bash
## Используйте SSH-туннель
ssh -L 18789:localhost:18789 user@remote-server.com

## Или используйте Tailscale Serve
clawdbot config set gateway.tailscale.mode serve
clawdbot gateway restart

## Доступ из локального браузера
http://localhost:18789
```

---

## Итоги урока

В этом уроке вы узнали:

1. ✅ **Введение в WebChat**: встроенный чат-интерфейс на основе Gateway WebSocket, без отдельной настройки
2. ✅ **Способы доступа**: браузер, приложение macOS, быстрый доступ из командной строки
3. ✅ **Основные функции**: управление сессиями, загрузка вложений, поддержка Markdown, команды со слэшем
4. ✅ **Удаленный доступ**: доступ к удаленному Gateway через SSH-туннель или Tailscale
5. ✅ **Настройка аутентификации**: понимание режимов аутентификации Gateway (token/password/Tailscale)
6. ✅ **Устранение неполадок**: распространенные проблемы и решения

**Обзор ключевых концепций**:

- WebChat использует тот же порт, что и Gateway, без отдельного HTTP-сервера
- История управляется Gateway, синхронизация в реальном времени, локально не кэшируется
- Если Gateway недоступен, WebChat переходит в режим только для чтения
- Ответы детерминированно маршрутизируются обратно в WebChat, в отличие от других каналов

**Следующие шаги**:

- Изучите [приложение macOS](../macos-app/), узнайте о управлении через меню и функции Voice Wake
- Узнайте об [узле iOS](../ios-node/), настройте выполнение локальных операций на мобильных устройствах
- Ознакомьтесь с [визуальным интерфейсом Canvas](../../advanced/canvas/), испытайте визуальное рабочее пространство с поддержкой AI

---

## Обзор следующего урока

> В следующем уроке мы изучим **[приложение macOS](../macos-app/)**.
>
> Вы узнаете:
> - Функции и структуру приложения для меню macOS
> - Использование Voice Wake и Talk Mode
> - Способ интеграции WebChat с приложением macOS
> - Инструменты отладки и управление удаленным Gateway

---

## Приложение: Справочник исходного кода

<details>
<summary><strong>Нажмите, чтобы раскрыть расположение исходного кода</strong></summary>

> Время обновления: 2026-01-27

| Функция | Путь к файлу | Строка |
| ------------------- | ------------------------------------------------------------------------------------------- | ------- |
| Объяснение принципа WebChat | [`docs/web/webchat.md`](https://github.com/clawdbot/clawdbot/blob/main/docs/web/webchat.md) | Весь файл |
| Gateway WebSocket API | [`src/gateway/protocol/`](https://github.com/clawdbot/clawdbot/blob/main/src/gateway/protocol/) | Весь каталог |
| Метод chat.send | [`src/gateway/server-methods/chat.ts`](https://github.com/clawdbot/clawdbot/blob/main/src/gateway/server-methods/chat.ts) | 296-380 |
| Метод chat.history | [`src/gateway/server-methods/chat.ts`](https://github.com/clawdbot/clawdbot/blob/main/src/gateway/server-methods/chat.ts) | 1-295 |
| Метод chat.inject | [`src/gateway/server-methods/chat.ts`](https://github.com/clawdbot/clawdbot/blob/main/src/gateway/server-methods/chat.ts) | 381-450 |
| Точка входа Web UI | [`ui/index.html`](https://github.com/clawdbot/clawdbot/blob/main/ui/index.html) | 1-15 |
| Настройка аутентификации Gateway | [`src/config/zod-schema.core.ts`](https://github.com/clawdbot/clawdbot/blob/main/src/config/zod-schema.core.ts) | 1-100 |
| Интеграция Tailscale | [`src/gateway/server-startup-log.ts`](https://github.com/clawdbot/clawdbot/blob/main/src/gateway/server-startup-log.ts) | Весь файл |
| Интеграция macOS WebChat | [`apps/macos/`](https://github.com/clawdbot/clawdbot/blob/main/apps/macos/) | Весь каталог |

**Ключевые константы**:
- `INTERNAL_MESSAGE_CHANNEL = "webchat"`: идентификатор внутреннего канала сообщений WebChat (из `src/utils/message-channel.ts:17`)

**Ключевые параметры настройки**:
- `gateway.port`: порт WebSocket (по умолчанию 18789)
- `gateway.auth.mode`: режим аутентификации (token/password/tailscale)
- `gateway.auth.token`: значение токена для token-аутентификации
- `gateway.auth.password`: значение пароля для аутентификации по паролю
- `gateway.tailscale.mode`: режим Tailscale (serve/funnel/disabled)
- `gateway.remote.url`: адрес WebSocket удаленного Gateway
- `gateway.remote.token`: токен аутентификации удаленного Gateway
- `gateway.remote.password`: пароль аутентификации удаленного Gateway

**Ключевые методы WebSocket**:
- `chat.send(message)`: отправка сообщения в Agent (из `src/gateway/server-methods/chat.ts`)
- `chat.history(sessionId)`: получение истории сессии (из `src/gateway/server-methods/chat.ts`)
- `chat.inject(message)`: прямая инъекция системной заметки в сессию, минуя Agent (из `src/gateway/server-methods/chat.ts`)

**Архитектурные особенности**:
- WebChat не требует отдельного HTTP-сервера или настройки портов
- Использует тот же порт, что и Gateway (по умолчанию 18789)
- История запрашивается из Gateway в реальном времени, локально не кэшируется
- Ответы детерминированно маршрутизируются обратно в WebChat (в отличие от других каналов)

</details>
