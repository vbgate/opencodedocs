---
title: "Конфигурация канала Signal: безопасная интеграция AI-ассистента на основе signal-cli | Учебник Clawdbot"
sidebarTitle: "Подключение приватного AI-ассистента Signal"
subtitle: "Конфигурация канала Signal: безопасная интеграция AI-ассистента на основе signal-cli | Учебник Clawdbot"
description: "Узнайте, как настроить канал Signal в Clawdbot, включая установку signal-cli, привязку аккаунта, поддержку нескольких аккаунтов, механизм спаривания DM, групповые сообщения и контроль доступа. В этом руководстве подробно описан полный процесс от установки до использования, помогая быстро создать персонального AI-ассистента на основе Signal."
tags:
  - "Signal"
  - "signal-cli"
  - "конфигурация канала"
  - "платформа сообщений"
prerequisite:
  - "start-getting-started"
order: 120
---

# Конфигурация канала Signal: использование signal-cli для подключения персонального AI-ассистента | Учебник Clawdbot

## Что вы сможете сделать после завершения

После завершения этого урока вы сможете:

- ✅ Установить и настроить signal-cli
- ✅ Настроить канал Signal в Clawdbot
- ✅ Взаимодействовать с AI-ассистентом через личные сообщения и группы
- ✅ Использовать механизм спаривания DM для защиты вашего аккаунта
- ✅ Настроить поддержку нескольких аккаунтов Signal
- ✅ Использовать индикатор набора, уведомления о прочтении и реакции Signal

## Ваша текущая проблема

Вы хотите использовать AI-ассистент в Signal, но столкнулись с этими проблемами:

- ❌ Не знаете, как подключить Signal и Clawdbot
- ❌ Беспокоитесь о конфиденциальности, не хотите загружать данные в облако
- ❌ Не уверены, как контролировать, кто может отправлять сообщения AI-ассистенту
- ❌ Нужно переключаться между несколькими аккаунтами Signal

::: info Почему стоит выбрать Signal?
Signal — это приложение для мгновенного обмена сообщениями с end-to-end шифрованием, все сообщения зашифрованы и могут быть прочитаны только отправителем и получателем. Clawdbot интегрируется через signal-cli, позволяя вам пользоваться функциями AI-ассистента, сохраняя конфиденциальность.
:::

## Когда использовать этот метод

**Сценарии, подходящие для использования канала Signal**:

- Вам нужен канал связи с приоритетом конфиденциальности
- Ваша команда или группа друзей использует Signal
- Вам нужно запустить AI-ассистент на личном устройстве (локально приоритет)
- Вам нужно контролировать доступ через защищенный механизм спаривания DM

::: tip Отдельный аккаунт Signal
Рекомендуется использовать **отдельный номер Signal** в качестве аккаунта бота, а не ваш личный номер. Это позволит избежать циклических сообщений (бот игнорирует свои собственные сообщения) и сохранить разделение между рабочими и личными коммуникациями.
:::

## Подготовка перед началом

Прежде чем начать, убедитесь, что вы выполнили следующие шаги:

::: warning Предварительные условия
- ✅ Завершен учебник [Быстрый старт](../../start/getting-started/)
- ✅ Clawdbot установлен и может нормально работать
- ✅ Настроен хотя бы один провайдер моделей AI (Anthropic, OpenAI, OpenRouter и т. д.)
- ✅ Установлена Java (требуется для signal-cli)
:::
## Основная концепция

Интеграция Signal в Clawdbot основана на **signal-cli** и работает следующим образом:

1. **Режим демона**: signal-cli работает как фоновый демон, предоставляя интерфейс HTTP JSON-RPC
2. **Поток событий (SSE)**: Clawdbot получает события Signal через Server-Sent Events (SSE)
3. **Стандартизация сообщений**: Сообщения Signal конвертируются в унифицированный внутренний формат, затем маршрутизируются к AI Agent
4. **Детерминированная маршрутизация**: Все ответы отправляются обратно отправителю или группе исходного сообщения

**Ключевые принципы дизайна**:

- **Локальный приоритет**: signal-cli работает на вашем устройстве, все коммуникации зашифрованы
- **Поддержка нескольких аккаунтов**: Можно настроить несколько аккаунтов Signal
- **Контроль доступа**: По умолчанию включен механизм спаривания DM, незнакомцы должны получить одобрение для отправки сообщений
- **Изоляция контекста**: Групповые сообщения имеют независимый контекст сессии, не смешиваются с личными сообщениями

## Следуйте моим инструкциям

### Шаг 1: Установка signal-cli

**Зачем**
signal-cli — это интерфейс командной строки для Signal, через него Clawdbot взаимодействует с сетью Signal.

**Методы установки**

::: code-group

```bash [macOS (Homebrew)]
brew install signal-cli
```

```bash [Linux (Ubuntu/Debian)]
# Посетите https://github.com/AsamK/signal-cli/releases для просмотра последней версии
# Скачайте последний пакет signal-cli (замените VERSION на фактический номер версии)
wget https://github.com/AsamK/signal-cli/releases/download/vVERSION/signal-cli-VERSION.tar.gz

# Распакуйте в каталог /opt
sudo tar -xvf signal-cli-VERSION.tar.gz -C /opt/

# Создайте символическую ссылку (необязательно)
sudo ln -s /opt/signal-cli-VERSION/bin/signal-cli /usr/local/bin/signal-cli
```

```bash [Windows (WSL2)]
# В WSL2 используйте метод установки для Linux
# Примечание: Windows использует WSL2, установка signal-cli следует процессу Linux
```

:::

**Проверка установки**

```bash
signal-cli --version
# Должен показать: номер версии signal-cli (например, 0.13.x или выше)
```

**Вы должны увидеть**: вывод номера версии signal-cli.

::: danger Требование к Java
signal-cli требует выполнения Java (обычно Java 11 или выше). Убедитесь, что Java установлена и настроена:

```bash
java -version
# Должен показать: openjdk version "11.x.x" или выше
```

**Примечание**: Для конкретных требований к версии Java обратитесь к [официальной документации signal-cli](https://github.com/AsamK/signal-cli#readme).
:::
### Шаг 2: Привязка аккаунта Signal

**Зачем**
После привязки аккаунта signal-cli сможет отправлять и получать сообщения от вашего номера Signal.

**Рекомендация**: Используйте отдельный номер Signal в качестве аккаунта бота.

**Шаги привязки**

1. **Сгенерировать команду привязки**:

```bash
signal-cli link -n "Clawdbot"
```

`-n "Clawdbot"` указывает имя устройства как "Clawdbot" (можно настроить).

2. **Сканировать QR-код**:

После выполнения команды в терминале отобразится QR-код:

```
tsconfig: 2369:35 - warning - *! is deprecated: Use .nonNull().
  (deprecated-non-null)

To link your device, navigate to
  Signal Settings > Linked Devices > (+) Link New Device
  on your phone and scan the QR code displayed below.

██████████████████████████████████████████████
██████████████████████████████████████████████
██████████████████████████████████████████████
██████████████████████████████████████████████
██████████████████████████████████████████████
...
```

3. **В приложении Signal на телефоне**:

   - Откройте настройки Signal
   - Выберите "Связанные устройства" (Linked Devices)
   - Нажмите "(+) Привязать новое устройство" (Link New Device)
   - Отсканируйте QR-код, отображаемый в терминале

**Вы должны увидеть**: после успешной привязки терминал отобразит примерно такой вывод:

```
INFO  Account restored (Number: +15551234567)
INFO  Configuration created at: ~/.local/share/signal-cli/data
```

::: tip Поддержка нескольких устройств
Signal позволяет привязать до 4 устройств. Clawdbot будет работать как одно из них. Вы можете просматривать и управлять этими устройствами в приложении Signal в разделе "Связанные устройства".
:::
### Шаг 3: Настройка канала Signal в Clawdbot

**Зачем**
Файл конфигурации сообщает Clawdbot, как подключиться к signal-cli и как обрабатывать сообщения из Signal.

**Методы настройки**

Есть три способа настройки, выберите подходящий для вас:

#### Способ 1: Быстрая настройка (один аккаунт)

Это самый простой способ, подходит для сценария с одним аккаунтом.

Отредактируйте `~/.clawdbot/clawdbot.json`:

```json
{
  "channels": {
    "signal": {
      "enabled": true,
      "account": "+15551234567",
      "cliPath": "signal-cli",
      "dmPolicy": "pairing",
      "allowFrom": ["+15557654321"]
    }
  }
}
```

**Описание полей**:

| Поле | Значение | Описание |
|------|---------|----------|
| `enabled` | `true` | Включить канал Signal |
| `account` | `"+15551234567"` | Ваш аккаунт Signal (формат E.164) |
| `cliPath` | `"signal-cli"` | Путь к команде signal-cli |
| `dmPolicy` | `"pairing"` | Политика доступа DM (режим спаривания по умолчанию) |
| `allowFrom` | `["+15557654321"]` | Белый список номеров, которым разрешено отправлять DM |

#### Способ 2: Настройка нескольких аккаунтов

Если вам нужно управлять несколькими аккаунтами Signal, используйте объект `accounts`:

```json
{
  "channels": {
    "signal": {
      "enabled": true,
      "accounts": {
        "work": {
          "account": "+15551234567",
          "name": "Work Bot",
          "httpPort": 8080,
          "dmPolicy": "pairing",
          "allowFrom": ["+15557654321"]
        },
        "personal": {
          "account": "+15559876543",
          "name": "Personal Bot",
          "httpPort": 8081,
          "dmPolicy": "allowlist",
          "allowFrom": ["*"]
        }
      }
    }
  }
}
```

**Описание настройки**:

- Каждый аккаунт имеет уникальный ID (например, `work`, `personal`)
- Каждый аккаунт может иметь разные порты, политики и разрешения
- `name` — необязательное отображаемое имя, используется в списках CLI/UI
#### Способ 3: Режим внешнего демона

Если вы хотите самостоятельно управлять signal-cli (например, в контейнере или при совместном использовании CPU), отключите автоматический запуск:

```json
{
  "channels": {
    "signal": {
      "enabled": true,
      "httpUrl": "http://127.0.0.1:8080",
      "autoStart": false
    }
  }
}
```

**Описание настройки**:

- `httpUrl`: полный URL демона (переопределяет `httpHost` и `httpPort`)
- `autoStart`: установите в `false`, чтобы отключить автоматический запуск signal-cli
- Clawdbot подключится к уже запущенному демону signal-cli

**Вы должны увидеть**: после сохранения файла конфигурации нет синтаксических ошибок.

::: tip Проверка конфигурации
Clawdbot проверяет конфигурацию при запуске. Если конфигурация неверна, в журналах отобразится подробное сообщение об ошибке.
:::

### Шаг 4: Запуск Gateway

**Зачем**
После запуска Gateway Clawdbot автоматически запустит демон signal-cli (если вы не отключили `autoStart`) и начнет прослушивать сообщения Signal.

**Команда запуска**

```bash
clawdbot gateway start
```

**Вы должны увидеть**: примерно такой вывод:

```
[INFO] Starting Clawdbot Gateway...
[INFO] Starting signal-cli daemon...
[INFO] signal-cli: INFO  Starting daemon...
[INFO] signal-cli: INFO  Daemon started on http://127.0.0.1:8080
[INFO] Signal channel ready (account: +15551234567)
[INFO] Gateway listening on ws://127.0.0.1:18789
[INFO] Clawdbot Gateway started successfully
```

**Проверка состояния канала**

```bash
clawdbot channels status
```

**Вы должны увидеть**: примерно такой вывод:

```
Signal Channels:
  ├─ +15551234567 (Work Bot)
  │   ├─ Status: Connected
  │   ├─ Daemon: http://127.0.0.1:8080
  │   └─ Mode: Auto-start
```
### Шаг 5: Отправка первого сообщения

**Зачем**
Проверить правильность конфигурации, убедиться, что Clawdbot может принимать и обрабатывать сообщения Signal.

**Отправка сообщения**

1. **Из приложения Signal на телефоне** отправьте сообщение номеру вашего бота:

```
Привет, Clawdbot!
```

2. **Обработка первого сообщения**:

   Если `dmPolicy="pairing"` (по умолчанию), незнакомцы получат код спаривания:

   ```
   ❌ Несанкционированный отправитель

   Чтобы продолжить, используйте следующий код для подтверждения спаривания:

   📝 Код спаривания: ABC123

   Код истечет через 1 час.

   Для подтверждения выполните:
   clawdbot pairing approve signal ABC123
   ```

3. **Подтверждение спаривания**:

   ```bash
   clawdbot pairing list signal
   ```

   Список ожидающих подтверждения запросов на спаривание:

   ```
   Pending Pairings (Signal):
     ├─ ABC123
     │   ├─ Sender: +15557654321
     │   ├─ UUID: uuid:123e4567-e89b-12d3-a456-426614174000
     │   └─ Expires: 2026-01-27 12:00:00
   ```

   Подтвердите спаривание:

   ```bash
   clawdbot pairing approve signal ABC123
   ```

4. **Отправка второго сообщения**:

   После успешного спаривания отправьте сообщение снова:

   ```
   Привет, Clawdbot!
   ```

**Вы должны увидеть**:

- В приложении Signal на телефоне получен ответ от AI:
  ```
  Привет! Я Clawdbot, ваш персональный AI-ассистент. Чем могу помочь?
  ```

- Журналы Gateway показывают:
  ```
  [INFO] Received Signal message from +15557654321
  [INFO] Processing message through Agent...
  [INFO] Sending Signal response to +15557654321
  ```
**Контрольная точка ✅**:

- [ ] Демон signal-cli запущен
- [ ] Состояние канала отображает "Connected"
- [ ] После отправки сообщения получен ответ от AI
- [ ] Журналы Gateway без сообщений об ошибках

::: danger Свои сообщения будут игнорироваться
Если вы запускаете бота на личном номере Signal, бот будет игнорировать сообщения, отправленные вами самим (защита от циклов). Рекомендуется использовать отдельный номер для бота.
:::

## Предупреждения о подводных камнях

### Проблема 1: Неудачный запуск signal-cli

**Проблема**: Демон signal-cli не запускается

**Возможные причины**:

1. Java не установлена или версия слишком старая
2. Порт уже занят
3. Неверный путь к signal-cli

**Решения**:

```bash
# Проверка версии Java
java -version

# Проверка занятости порта
lsof -i :8080  # macOS/Linux
netstat -ano | findstr :8080  # Windows (PowerShell)

# Проверка пути signal-cli
which signal-cli
```

### Проблема 2: Код спаривания истек

**Проблема**: Код спаривания истекает через 1 час

**Решение**:

Отправьте сообщение снова, получите новый код спаривания и подтвердите его в течение 1 часа.

### Проблема 3: Нет ответа на групповые сообщения

**Проблема**: @упоминание бота в группе Signal, но нет ответа

**Возможные причины**:

- `groupPolicy` установлена в `allowlist`, но вы не находитесь в `groupAllowFrom`
- Signal не поддерживает встроенные @упоминания (в отличие от Discord/Slack)

**Решения**:

Настройте групповую политику:

```json
{
  "channels": {
    "signal": {
      "groupPolicy": "allowlist",
      "groupAllowFrom": ["+15557654321"]
    }
  }
}
```

Или используйте командный триггер (если настроено `commands.config: true`):

```
@clawdbot help
```
### Проблема 4: Неудачная загрузка медиа-файлов

**Проблема**: Изображения или вложения в сообщениях Signal не могут быть обработаны

**Возможные причины**:

- Размер файла превышает ограничение `mediaMaxMb` (по умолчанию 8MB)
- `ignoreAttachments` установлено в `true`

**Решения**:

```json
{
  "channels": {
    "signal": {
      "mediaMaxMb": 20,
      "ignoreAttachments": false
    }
  }
}
```

### Проблема 5: Длинные сообщения обрезаются

**Проблема**: Отправляемые сообщения разделяются на несколько частей

**Причина**: Signal имеет ограничение на длину сообщения (по умолчанию 4000 символов), Clawdbot автоматически разбивает сообщения

**Решения**:

Настройте разбиение на части:

```json
{
  "channels": {
    "signal": {
      "textChunkLimit": 2000,
      "chunkMode": "newline"
    }
  }
}
```

Опции `chunkMode`:
- `length` (по умолчанию): разбивка по длине
- `newline`: сначала разбивка по пустым строкам (границы абзацев), затем по длине
## Итоги урока

В этом уроке мы завершили настройку и использование канала Signal:

**Основные концепции**:
- Канал Signal основан на signal-cli, общается через HTTP JSON-RPC + SSE
- Рекомендуется использовать отдельный номер бота, чтобы избежать циклических сообщений
- По умолчанию включен механизм спаривания DM для защиты безопасности вашего аккаунта

**Ключевые настройки**:
- `account`: аккаунт Signal (формат E.164)
- `cliPath`: путь к signal-cli
- `dmPolicy`: политика доступа DM (по умолчанию `pairing`)
- `allowFrom`: белый список DM
- `groupPolicy` / `groupAllowFrom`: групповая политика

**Полезные функции**:
- Поддержка нескольких аккаунтов
- Групповые сообщения (независимый контекст)
- Индикатор набора
- Уведомления о прочтении
- Реакции (emoji реакции)

**Устранение неполадок**:
- Используйте `clawdbot channels status` для проверки состояния канала
- Используйте `clawdbot pairing list signal` для просмотра ожидающих подтверждения запросов на спаривание
- Просматривайте журналы Gateway для получения подробных сообщений об ошибках

## Предварительный обзор следующего урока

> В следующем уроке мы изучим **[Канал iMessage](../imessage/)**.
>
> Вы узнаете:
> - Как настроить канал iMessage на macOS
> - Использование расширения BlueBubbles
> - Специальные функции iMessage (уведомления о прочтении, индикатор набора и т. д.)
> - Интеграция узлов iOS (Camera, Canvas, Voice Wake)

Продолжайте исследовать мощные функции Clawdbot! 🚀
---

## Приложение: Ссылки на исходный код

<details>
<summary><strong>Нажмите, чтобы развернуть</strong></summary>

> Обновлено: 2026-01-27

| Функция | Путь к файлу | Строки |
| ----------- | ------------------------------------------------------------------------------------------- | ------- |
| RPC клиент Signal | [`src/signal/client.ts`](https://github.com/clawd/clawdbot/blob/main/src/signal/client.ts)         | 1-186   |
| Управление демоном Signal | [`src/signal/daemon.ts`](https://github.com/clawd/clawdbot/blob/main/src/signal/daemon.ts)         | 1-85    |
| Поддержка нескольких аккаунтов | [`src/signal/accounts.ts`](https://github.com/clawd/clawdbot/blob/main/src/signal/accounts.ts)       | 1-84    |
| Мониторинг и обработка событий Signal | [`src/signal/monitor.ts`](https://github.com/clawd/clawdbot/blob/main/src/signal/monitor.ts)       | -       |
| Отправка сообщений | [`src/signal/send.ts`](https://github.com/clawd/clawdbot/blob/main/src/signal/send.ts)             | -       |
| Отправка реакций | [`src/signal/send-reactions.ts`](https://github.com/clawd/clawdbot/blob/main/src/signal/send-reactions.ts) | -       |
| Конфигурация уровня реакции | [`src/signal/reaction-level.ts`](https://github.com/clawd/clawdbot/blob/main/src/signal/reaction-level.ts) | -       |

**Определения типов конфигурации**:
- `SignalAccountConfig`: [`src/config/types.signal.ts:13-87`](https://github.com/clawd/clawdbot/blob/main/src/config/types.signal.ts#L13-L87)
- `SignalConfig`: [`src/config/types.signal.ts:89-93`](https://github.com/clawd/clawdbot/blob/main/src/config/types.signal.ts#L89-L93)

**Ключевые константы**:
- `DEFAULT_TIMEOUT_MS = 10_000`: время ожидания по умолчанию для запросов Signal RPC (10 секунд) источник: `src/signal/client.ts:29`
- Порт HTTP по умолчанию: `8080` источник: `src/signal/accounts.ts:59`
- Размер разбиения текста по умолчанию: `4000` символов источник: `docs/channels/signal.md:113`

**Ключевые функции**:
- `signalRpcRequest<T>()`: отправка JSON-RPC запроса в signal-cli источник: `src/signal/client.ts:54-90`
- `streamSignalEvents()`: подписка на события Signal через SSE источник: `src/signal/client.ts:112-185`
- `spawnSignalDaemon()`: запуск демона signal-cli источник: `src/signal/daemon.ts:50-84`
- `resolveSignalAccount()`: разрешение конфигурации аккаунта Signal источник: `src/signal/accounts.ts:49-77`
- `listEnabledSignalAccounts()`: список включенных аккаунтов Signal источник: `src/signal/accounts.ts:79-83`

</details>
